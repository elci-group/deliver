# gettext pair where the fr plural entry has msgstr[0] filled but msgstr[1]
# empty: the declared nplurals=2 has only 1 translated arm, so
# OGMA-I18N-004 (Error) fails fr's structural_integrity check. fr is
# non-native; overall FAIL.
description = "gettext fr plural entry with an empty msgstr[1]; OGMA-I18N-004, FAIL"

[policy]
allow_fallback = true

[expect]
baseline = "none"
baseline_confidence = "UNKNOWN"
locales = ["en", "fr"]
overall = "FAIL"

[expect.native]
en = true

[expect.violations]
global = []
en = []
fr = ["OGMA-I18N-004"]
