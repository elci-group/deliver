Implemented `parse_percent` in `calculator.py` with whitespace and `%` handling, range validation, and `ValueError` for invalid input.

All 3 tests pass. Protected files were unchanged, and deliver was not invoked.