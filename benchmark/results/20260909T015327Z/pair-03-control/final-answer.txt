Implemented `parse_percent` in `calculator.py` with whitespace and optional `%` handling, range validation, and `ValueError` for invalid inputs.

All 3 tests and additional boundary checks passed. Protected files were unchanged; deliver was not used.