# Benchmark task

Work only in `/home/sal/deliver/benchmark/results/20260908T204029Z/pair-02-deliver/project`. Read that directory's `README.md`, then implement the requested behaviour in `calculator.py`. Do not modify `README.md`, `test_calculator.py`, or `deliver.toml`.

Before your final response, run `deliver --spec deliver.toml --strict` and fix any failure.

Keep the change focused and leave the working tree in a state that satisfies the task's acceptance checks.
