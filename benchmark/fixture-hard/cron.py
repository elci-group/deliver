"""Cron expression next-occurrence calculator."""


def next_occurrence(expr: str, start):
    """Return the first time strictly after `start` matching the cron expr."""
    raise NotImplementedError("implement next_occurrence")
