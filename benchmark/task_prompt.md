# Benchmark task

Work only in `{workdir}`. Read that directory's `README.md`, then implement the requested behaviour in `calculator.py`. Do not modify `README.md`, `test_calculator.py`, or `deliver.toml`.

{condition_instruction}

Keep the change focused and leave the working tree in a state that satisfies the task's acceptance checks.
